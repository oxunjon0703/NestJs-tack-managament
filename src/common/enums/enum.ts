export enum RoleEnum {
  superAdmin = 'superAdmin',
  admin = 'admin',
  manager = 'manager',
  worker = 'worker',
}

export enum StatusEnum {
  took = 'took',
  done = 'done',
  process = 'process',
}
